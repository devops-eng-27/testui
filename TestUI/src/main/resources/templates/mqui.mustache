{{>layouts/header}}

<h1 style="padding: 30px 0px 0px 30px;"><b>KAFKA MQ Service</b></h1>
<h5 style="padding: 0px 0px 30px 30px;">실시간 메시지 처리에 특화된 Kafka MQ 서비스</h5>

<div class="col-md-12" style="padding: 0px 0px 0px 30px;">
    <div class="col-md-4">
        <a href="/post" class="btn btn-primary" role="button" methods="get">MQ생성</a>
        <a href="/delete" role="button" class="btn btn-primary" methods="post">MQ삭제</a>
        <a href="/agent" role="button" class="btn btn-primary" methods="post">KAFKA</a>
    </div>
</div>

<div style="padding: 30px 30px 0px 30px;">
<table class="table table-hover">
    <thead class="thread-strong">
    <tr>
        <th scope="col">이름</th>
        <th scope="col">위치</th>
        <th scope="col">사양</th>
        <th scope="col">Kafka 버전</th>
        <th scope="col">볼륨 용량</th>
        <th scope="col">broker 개수</th>
        <th scope="col">IP Address</th>
        <th scope="col">Kafdrop UI</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <td>{{post.name}}</td>
        <td>{{post.location}}</td>
        <td>{{post.cpu}}</td>
        <td>{{post.version}}</td>
        <td>{{post.volume}}</td>
        <td>{{post.broker}}</td>
        <td>{{post.ip}}</td>
        <td>{{post.ip}}:9000</td>
    </tr>
    </tbody>
</table>
</div>
{{>layouts/footer}}